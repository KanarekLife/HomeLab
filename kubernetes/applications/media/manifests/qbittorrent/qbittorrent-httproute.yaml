apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: downloads-httproute
  namespace: authentik
spec:
  parentRefs:
    - name: default-gateway
      namespace: kube-system
  hostnames:
    - downloads.homelab.lan.nieradko.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ak-outpost-qbittorrent-outpost
          port: 9000
